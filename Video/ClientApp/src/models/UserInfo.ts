export interface UserInfo {
    id: number,
    token: string,
    firstName: string,
    lastName: string
}

export interface User {
    userName: string;
    email: string;
    id: number,
    firstName: string;
    lastName: string;
}